#include "monty.h"
/**
  *df_nop- this is nothing
  *@head: the stack head
  *@counter: the line_number
  *Return: it will give no return
 */
void df_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
